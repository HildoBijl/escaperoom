// Prize registration flag.
// The deadline (10 July 2026) has passed and will not be postponed, so prize
// entry is closed. Flip to true only if a new prize round is opened; all
// prize-related UI reads from this single switch.
export const PRIZES_OPEN: boolean = false;
